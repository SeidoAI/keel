1. Incorporate superpowers skill + tripwire. Need to define. Ideally, should integrate with it - so can benefit from updates to superpowers. Also, pulls users of superpowers - "integrates perfectly with superpowers planning skill"
2. Make validate --strict the default - seems more used than non-strict.
3. PM prompts should dynamically load config. I.e. if certain artifacts are required - these shouldn't be hardcoded but loaded dynamically. "Skill adapts dynamically to config"
4. Tripwires as a first-class primitive — see full spec at `docs/specs/2026-04-21-v08-tripwires-as-primitive.md`. Adds a third lifecycle primitive alongside phases and artifacts: content invisible to the executing agent until a registered lifecycle event fires it, blocking the event until acknowledged. The four-lens self-review (originally drafted as just a session-complete hook) becomes the canonical first instance. Naming alignment: the tool's name now matches its central abstraction.

5. Bug: `tripwire session complete` writes `status: done` to session.yaml, but the SessionStatus enum (both packaged and project-overridden) doesn't include `done` — the terminal state is `completed`. Found 2026-04-21 when completing backend-{read-services,realtime,v2-stubs} after PRs #8/#9/#10 merged. Workaround: hand-edit session.yaml `done → completed` after running `tripwire session complete`. Real fix: pick one (either add `done` to the enum and migrate `completed` usage, or change the CLI to write `completed`). Decision pending — the v0.7b "6-stage flow" intent isn't clear from the merged PR; check the v0.7b unified design spec.

6. Split `src/tripwire/cli/session.py` (~1400 lines after v0.7b) into per-subcommand modules: `cli/session_monitor_cmd.py`, `cli/session_review_cmd.py`, `cli/session_complete_cmd.py`, `cli/session_insights_cmd.py`. Move helpers (`_gather_pr_number`, `_gather_pr_files`, `_write_verified_for_session`, `_write_review_json`) with them. Bloat introduced in v0.7b; no behaviour change. Carried over as deferred from v0.7b self-review.

7. End-to-end integration test for the full session lifecycle: `queue → spawn → monitor → review → complete`. v0.7b shipped unit tests per phase; nothing exercises the whole chain as one sequence. Coverage gap called out in v0.7b self-review. Should stub claude + gh subprocess calls; use the shared `_init_repo` fixture (already portability-fixed in the v0.7.0 CI-fix commit).

8. `tripwire session insights defer` action. Spec §7.8 lists three PM actions (accept, reject, defer); v0.7b shipped only accept and reject. `defer` records a `deferred.yaml` entry with a link to a follow-up curation issue key and drops the proposal from `insights.yaml`. Rationale from spec: "interesting but needs more context — the PM logs a new curation issue and dismisses for now." Needs: model (`DeferredProposal` or extension of `NodeProposal`), store (`insights_deferred.yaml`), CLI (`tripwire session insights defer <sid> --proposal <id> --follow-up-issue <key> [--reason TEXT]`), docs.

9. PyPI long-description fix is in the tree (pyproject `readme` field + absolute image URLs in README hero) but only takes effect when a new version is published. Roll into whatever release carries the README refactor (e.g. v0.7.1 as a metadata-only bump, or v0.8.0 if the refactor waits).

10. README refactor — v0.7-aware rewrite. Drop "Four principles" duplicate under "How it works"; refresh slash-command table (current set = 13 v0.7 commands: session-{queue,spawn,check,agenda,monitor,review,complete,create,progress}, issue-{close,artifact}, project-{create,sync}); rewrite project-layout block to match v0.7 (`issues/<KEY>/{issue.yaml,developer.md,verified.md,comments/}`, `nodes/*.yaml` at top level, `sessions/<id>/{handoff.yaml,insights.yaml,review.json}`); fix stale counts ("v0.2, 502 tests" → v0.7.0 / 1282 tests; "14-check validation" → actual count from grep on validator.py); move "What we learned" (95 lines) to `docs/learnings.md` and link; drop the orchestration YAML example that references `events.on_session_complete` (not implemented). Add a one-line note on the PyPI dist/CLI name split.